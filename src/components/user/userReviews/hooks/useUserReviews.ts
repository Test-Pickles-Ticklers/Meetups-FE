import { useState } from "react";

const useUserReviews = () => {
  const [reviews, setReviews] = useState<ReviewModel[]>();

  const reviewList = () => {
    try {
    } catch (error) {}
  };

  const reviewUpdated = () => {};
};
